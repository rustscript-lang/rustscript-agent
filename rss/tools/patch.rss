use self::types as types;
use bytes;
use json;

fn map_int(value: map, key: string, fallback: int) -> int {
    let mut result: int = fallback;
    if value.has(key) {
        if type(value[key]) == "int" {
            let coerced: int = value[key];
            result = coerced;
        }
    }
    result
}

fn map_bool(value: map, key: string, fallback: bool) -> bool {
    let mut result: bool = fallback;
    if value.has(key) {
        if type(value[key]) == "bool" {
            let coerced: bool = value[key];
            result = coerced;
        }
    }
    result
}

fn int_to_string(value: int) -> string {
    let encoded: string = json::encode(value);
    encoded
}

fn starts_with(value: string, prefix: string) -> bool {
    let mut result: bool = false;
    if prefix.length <= value.length {
        result = value[0:prefix.length] == prefix;
    }
    result
}

fn char_at(value: string, index: int) -> string {
    value[index:(index + 1)]
}

fn unpublished() -> map {
    { publication: "not_published" }
}

fn fail(code: string, message: string, data: map) -> map {
    let mut mapped: string = message;
    if code == "cancelled" {
        mapped = "tool execution was cancelled";
    }
    if code == "deadline_elapsed" {
        mapped = "tool deadline elapsed";
    }
    {
        ok: false,
        content: "",
        data: data,
        error: {
            code: code,
            message: mapped
        },
        truncated: false,
        artifacts: []
    }
}

fn succeed(content: string, data: map, truncated: bool, artifacts: array) -> map {
    {
        ok: true,
        content: content,
        data: data,
        error: null,
        truncated: truncated,
        artifacts: artifacts
    }
}

fn fail_host(envelope: map) -> map {
    let error: map = types::map_map(envelope, "error");
    let mut code: string = types::map_string(error, "code", "internal_error");
    let mut message: string = types::map_string(error, "message", "capability failed");
    if message == "symlinks are not followed" {
        message = "operating-system operation failed";
    }
    if code == "wrong_type" {
        code = "path_denied";
    }
    if code == "budget_exceeded" {
        if message == "requested write exceeds the configured bound" {
            message = "write budget exceeded";
        }
    }
    let mut data: map = unpublished();
    if code == "publication_indeterminate" {
        data = { publication: "indeterminate" };
    }
    fail(code, message, data)
}

fn deny_path(message: string) -> map {
    { ok: false, code: "path_denied", message: message }
}

fn ok_path() -> map {
    { ok: true, code: "", message: "" }
}

fn path_has_slash(path: string) -> bool {
    let mut found: bool = false;
    let mut index: int = 0;
    while found == false && index < path.length {
        if char_at(path, index) == "/" {
            found = true;
        }
        index = index + 1;
    }
    found
}

fn validate_leaf_name(name: string) -> map {
    let mut result: map = ok_path();
    if utf8_len(name) > 255 {
        result = deny_path("name exceeds the hard bound");
    }
    if map_bool(result, "ok", false) {
        let mut index: int = 0;
        while map_bool(result, "ok", false) && index < name.length {
            if char_at(name, index) == "\0" {
                result = deny_path("name contains a NUL byte");
            }
            if char_at(name, index) == "\\" {
                result = deny_path("path separators are not permitted in one name");
            }
            if char_at(name, index) == ":" {
                result = deny_path("drive and prefix syntax is not permitted");
            }
            index = index + 1;
        }
    }
    if map_bool(result, "ok", false) {
        if name == "." || name == ".." {
            result = deny_path("dot and parent names are not permitted");
        }
    }
    if map_bool(result, "ok", false) {
        if char_at(name, name.length - 1) == "." {
            result = deny_path("trailing-dot names are not permitted");
        }
    }
    result
}

fn validate_tool_path(path: string, allow_empty: bool) -> map {
    let mut result: map = ok_path();
    if path.length == 0 {
        if allow_empty == false {
            result = deny_path("empty names are not permitted");
        }
    } else {
        if path_has_slash(path) {
            if utf8_len(path) > 4096 {
                result = deny_path("relative path exceeds the hard bound");
            }
            if map_bool(result, "ok", false) {
                let mut index: int = 0;
                while map_bool(result, "ok", false) && index < path.length {
                    if char_at(path, index) == "\0" {
                        result = deny_path("path contains a NUL byte");
                    }
                    index = index + 1;
                }
            }
            if map_bool(result, "ok", false) {
                if starts_with(path, "/") {
                    result = deny_path("rooted or trailing-separator paths are not permitted");
                }
            }
            if map_bool(result, "ok", false) {
                if char_at(path, path.length - 1) == "/" {
                    result = deny_path("rooted or trailing-separator paths are not permitted");
                }
            }
            if map_bool(result, "ok", false) {
                let mut index: int = 0;
                while map_bool(result, "ok", false) && index < path.length {
                    if char_at(path, index) == "\\" {
                        result = deny_path("backslash is not a permitted path separator");
                    }
                    if char_at(path, index) == ":" {
                        result = deny_path("drive and prefix syntax is not permitted");
                    }
                    index = index + 1;
                }
            }
            if map_bool(result, "ok", false) {
                let mut start: int = 0;
                let mut index: int = 0;
                while map_bool(result, "ok", false) && index <= path.length {
                    let at_end: bool = index == path.length;
                    let mut is_sep: bool = false;
                    if at_end == false {
                        if char_at(path, index) == "/" {
                            is_sep = true;
                        }
                    }
                    if at_end || is_sep {
                        if index == start {
                            result = deny_path("empty path components are not permitted");
                        } else {
                            let component: string = path[start:index];
                            if component == "." || component == ".." {
                                result = deny_path("dot and parent components are not permitted");
                            }
                            if map_bool(result, "ok", false) {
                                if char_at(component, component.length - 1) == "." {
                                    result = deny_path("trailing-dot components are not permitted");
                                }
                            }
                            if map_bool(result, "ok", false) {
                                if utf8_len(component) > 255 {
                                    result = deny_path("path component exceeds the hard bound");
                                }
                            }
                            start = index + 1;
                        }
                    }
                    index = index + 1;
                }
            }
        } else {
            result = validate_leaf_name(path);
        }
    }
    result
}

fn split_inclusive_newline(text: string) -> array {
    let mut lines: array = [];
    if text.length > 0 {
        let mut start: int = 0;
        let mut index: int = 0;
        while index < text.length {
            if char_at(text, index) == "\n" {
                lines[lines.length] = text[start:(index + 1)];
                start = index + 1;
            }
            index = index + 1;
        }
        if start < text.length {
            lines[lines.length] = text[start:text.length];
        }
    }
    lines
}

fn bytes_contains_nul(data: array) -> bool {
    let mut found: bool = false;
    let mut index: int = 0;
    while found == false && index < data.length {
        let byte: int = data[index];
        if byte == 0 {
            found = true;
        }
        index = index + 1;
    }
    found
}

fn utf8_is_valid(data: array) -> bool {
    let mut valid: bool = true;
    let mut index: int = 0;
    while valid && index < data.length {
        let byte: int = data[index];
        if byte < 0 || byte > 255 {
            valid = false;
        } else {
            if byte <= 127 {
                index = index + 1;
            } else {
                if byte >= 194 && byte <= 223 {
                    if index + 1 >= data.length {
                        valid = false;
                    } else {
                        let cont: int = data[index + 1];
                        if cont < 128 || cont > 191 {
                            valid = false;
                        } else {
                            index = index + 2;
                        }
                    }
                } else {
                    if byte >= 224 && byte <= 239 {
                        if index + 2 >= data.length {
                            valid = false;
                        } else {
                            let c1: int = data[index + 1];
                            let c2: int = data[index + 2];
                            if c1 < 128 || c1 > 191 || c2 < 128 || c2 > 191 {
                                valid = false;
                            } else {
                                if byte == 224 {
                                    if c1 < 160 {
                                        valid = false;
                                    }
                                }
                                if byte == 237 {
                                    if c1 > 159 {
                                        valid = false;
                                    }
                                }
                                if valid {
                                    index = index + 3;
                                }
                            }
                        }
                    } else {
                        if byte >= 240 && byte <= 244 {
                            if index + 3 >= data.length {
                                valid = false;
                            } else {
                                let d1: int = data[index + 1];
                                let d2: int = data[index + 2];
                                let d3: int = data[index + 3];
                                if d1 < 128 || d1 > 191 || d2 < 128 || d2 > 191 || d3 < 128 || d3 > 191 {
                                    valid = false;
                                } else {
                                    if byte == 240 {
                                        if d1 < 144 {
                                            valid = false;
                                        }
                                    }
                                    if byte == 244 {
                                        if d1 > 143 {
                                            valid = false;
                                        }
                                    }
                                    if valid {
                                        index = index + 4;
                                    }
                                }
                            }
                        } else {
                            valid = false;
                        }
                    }
                }
            }
        }
    }
    valid
}

fn control_failure() -> map {
    let control: map = agent::control_check();
    if map_bool(control, "ok", false) => {
        { ok: true, code: "", message: "" }
    } else => {
        let error: map = types::map_map(control, "error");
        {
            ok: false,
            code: types::map_string(error, "code", "cancelled"),
            message: types::map_string(error, "message", "cancelled")
        }
    }
}

fn encoded_len(result: map) -> int {
    let encoded: string = json::encode(result);
    utf8_len(encoded)
}

fn utf8_len(value: string) -> int {
    let payload: bytes = bytes::from_utf8(value);
    payload.length
}

fn truncate_to_utf8_bytes(text: string, limit: int) -> string {
    let mut end: int = 0;
    let mut used: int = 0;
    let mut walking: bool = true;
    while walking && end < text.length {
        let ch: string = char_at(text, end);
        let ch_bytes: int = utf8_len(ch);
        if used + ch_bytes > limit {
            walking = false;
        } else {
            used = used + ch_bytes;
            end = end + 1;
        }
    }
    text[0:end]
}

fn fit_content_to_budget(result: map, cap: int) -> map {
    let content: string = types::map_string(result, "content", "");
    let mut fitted: map = result;
    fitted.content = "";
    fitted.truncated = true;
    if encoded_len(fitted) <= cap {
        let mut budget: int = cap - encoded_len(fitted);
        let mut walking: bool = true;
        while walking {
            fitted.content = truncate_to_utf8_bytes(content, budget);
            fitted.truncated = true;
            if encoded_len(fitted) <= cap {
                walking = false;
            } else {
                if budget <= 0 {
                    fitted.content = "";
                    walking = false;
                } else {
                    budget = budget / 2;
                }
            }
        }
    }
    fitted
}

fn artifact_summary(id: string, bytes: int, max_output_bytes: int) -> string {
    let full: string = "artifact " + id + " (" + int_to_string(bytes) + " bytes)";
    let mut summary: string = full;
    if utf8_len(full) > max_output_bytes {
        summary = "artifact " + id;
        if utf8_len(summary) > max_output_bytes {
            summary = "artifact";
            if utf8_len(summary) > max_output_bytes {
                summary = truncate_to_utf8_bytes("artifact", max_output_bytes);
            }
        }
    }
    summary
}

fn shrink_result(result: map, max_output_bytes: int, token: string) -> map {
    let mut current: map = result;
    if encoded_len(current) > max_output_bytes {
        let content: string = types::map_string(current, "content", "");
        let data: map = types::map_map(current, "data");
        if content.length > 0 {
            let payload: bytes = bytes::from_utf8(content);
            let put: map = cap::artifact_put_result(token, payload, {});
            if map_bool(put, "ok", false) {
                let id: string = types::map_string(put, "id", "");
                let len: int = map_int(put, "len", utf8_len(content));
                let mut artifacts: array = [];
                artifacts[0] = id;
                current = succeed(artifact_summary(id, len, max_output_bytes), data, true, artifacts);
            }
        }
        if encoded_len(current) > max_output_bytes {
            current = fit_content_to_budget(current, max_output_bytes);
        }
        if encoded_len(current) > max_output_bytes {
            current = fail("output_truncated", "tool result exceeds the configured bound", unpublished());
            current.truncated = true;
            if encoded_len(current) > max_output_bytes {
                current = fail("output_truncated", "bounded", unpublished());
                current.truncated = true;
                if encoded_len(current) > max_output_bytes {
                    current = fail("output_truncated", "", unpublished());
                    current.truncated = true;
                }
            }
        }
    }
    current
}

fn finish_truncated(preview: string, max_bytes: int) -> string {
    let mut truncated: string = preview;
    if utf8_len(preview) > max_bytes {
        if max_bytes < 3 {
            truncated = truncate_to_utf8_bytes(preview, max_bytes);
        } else {
            truncated = truncate_to_utf8_bytes(preview, max_bytes - 3) + "…";
        }
    }
    truncated
}

fn trim_end_newline(line: string) -> string {
    let mut trimmed: string = line;
    if line.length > 0 {
        if char_at(line, line.length - 1) == "\n" {
            trimmed = line[0:(line.length - 1)];
        }
    }
    trimmed
}

fn push_diff_line(preview: string, marker: string, line: string, max_bytes: int) -> map {
    let next: string = preview + marker + trim_end_newline(line) + "\n";
    let mut pushed: map = { preview: next, ok: true };
    if utf8_len(next) <= max_bytes {
        pushed = { preview: next, ok: true };
    } else {
        pushed = { preview: finish_truncated(next, max_bytes), ok: false };
    }
    pushed
}

fn bounded_diff(path: string, before: string, after: string, max_bytes: int) -> string {
    let mut preview: string = "diff --git a/" + path + " b/" + path + "\n--- a/" + path + "\n+++ b/" + path + "\n";
    if utf8_len(preview) > max_bytes {
        preview = finish_truncated(preview, max_bytes);
    } else {
        let before_lines: array = split_inclusive_newline(before);
        let after_lines: array = split_inclusive_newline(after);
        let mut index: int = 0;
        let mut common: int = before_lines.length;
        if after_lines.length < common {
            common = after_lines.length;
        }
        let mut fitting: bool = true;
        while fitting && index < common {
            let old: string = before_lines[index].copy();
            let new: string = after_lines[index].copy();
            if old != new {
                let minus: map = push_diff_line(preview, "-", old, max_bytes);
                preview = types::map_string(minus, "preview", preview);
                if map_bool(minus, "ok", false) == false {
                    fitting = false;
                } else {
                    let plus: map = push_diff_line(preview, "+", new, max_bytes);
                    preview = types::map_string(plus, "preview", preview);
                    if map_bool(plus, "ok", false) == false {
                        fitting = false;
                    }
                }
            }
            index = index + 1;
        }
        if fitting {
            if before_lines.length < after_lines.length {
                let mut extra: int = before_lines.length;
                while fitting && extra < after_lines.length {
                    let line: string = after_lines[extra].copy();
                    let plus: map = push_diff_line(preview, "+", line, max_bytes);
                    preview = types::map_string(plus, "preview", preview);
                    if map_bool(plus, "ok", false) == false {
                        fitting = false;
                    }
                    extra = extra + 1;
                }
            } else {
                if after_lines.length < before_lines.length {
                    let mut extra: int = after_lines.length;
                    while fitting && extra < before_lines.length {
                        let line: string = before_lines[extra].copy();
                        let minus: map = push_diff_line(preview, "-", line, max_bytes);
                        preview = types::map_string(minus, "preview", preview);
                        if map_bool(minus, "ok", false) == false {
                            fitting = false;
                        }
                        extra = extra + 1;
                    }
                }
            }
        }
        if fitting {
            if utf8_len(preview) > max_bytes {
                preview = finish_truncated(preview, max_bytes);
            }
        }
    }
    preview
}

fn count_matches(source: string, needle: string) -> map {
    let mut count: int = 0;
    let mut index: int = 0;
    let mut scanned: int = 0;
    let mut result: map = { ok: true, count: 0, code: "", message: "" };
    while map_bool(result, "ok", false) && index + needle.length <= source.length {
        if source[index:(index + needle.length)] == needle {
            count = count + 1;
            index = index + needle.length;
        } else {
            index = index + 1;
        }
        scanned = scanned + 1;
        if scanned == 64 {
            scanned = 0;
            let checked: map = control_failure();
            if map_bool(checked, "ok", false) == false {
                result = {
                    ok: false,
                    count: 0,
                    code: types::map_string(checked, "code", "cancelled"),
                    message: types::map_string(checked, "message", "")
                };
            }
        }
    }
    if map_bool(result, "ok", false) {
        result.count = count;
    }
    result
}

fn replace_text(source: string, old: string, new: string, limit: int) -> string {
    let mut out: string = "";
    let mut start: int = 0;
    let mut index: int = 0;
    let mut replaced: int = 0;
    while index + old.length <= source.length {
        if replaced == limit {
            index = source.length;
        } else {
            if source[index:(index + old.length)] == old {
                out = out + source[start:index] + new;
                index = index + old.length;
                start = index;
                replaced = replaced + 1;
            } else {
                index = index + 1;
            }
        }
    }
    out = out + source[start:source.length];
    out
}

pub fn descriptor() -> map {
    types::patch_descriptor()
}

pub fn validate(arguments: map) -> map {
    let mut result: map = { ok: true, code: "", message: "" };
    if arguments.has("path") == false {
        result = { ok: false, code: "invalid_arguments", message: "patch requires path" };
    } else {
        if type(arguments.path) != "string" {
            result = { ok: false, code: "invalid_arguments", message: "patch requires path" };
        } else {
            result = validate_tool_path(arguments.path, false);
        }
    }
    if map_bool(result, "ok", false) {
        if arguments.has("old_string") == false {
            result = { ok: false, code: "invalid_arguments", message: "patch requires old_string" };
        } else {
            if type(arguments.old_string) != "string" {
                result = { ok: false, code: "invalid_arguments", message: "patch requires old_string" };
            }
        }
    }
    if map_bool(result, "ok", false) {
        if arguments.has("new_string") == false {
            result = { ok: false, code: "invalid_arguments", message: "patch requires new_string" };
        } else {
            if type(arguments.new_string) != "string" {
                result = { ok: false, code: "invalid_arguments", message: "patch requires new_string" };
            }
        }
    }
    if map_bool(result, "ok", false) {
        if arguments.has("old_string") {
            if type(arguments.old_string) == "string" {
                let old_string: string = arguments.old_string;
                if old_string.length == 0 {
                    result = { ok: false, code: "invalid_arguments", message: "patch old_string must be non-empty" };
                }
            }
        }
    }
    result
}

pub fn execute(context: map, arguments: map) -> map {
    let token: string = types::map_string(context, "execution_token", "");
    let config: map = types::map_map(context, "config");
    let path: string = types::map_string(arguments, "path", "");
    let old_string: string = types::map_string(arguments, "old_string", "");
    let new_string: string = types::map_string(arguments, "new_string", "");
    let mut replace_all: bool = false;
    if arguments.has("replace_all") {
        if type(arguments.replace_all) == "bool" {
            replace_all = arguments.replace_all;
        }
    }
    let mut result: map = { ok: true, content: "", data: {}, error: null, truncated: false, artifacts: [] };
    let checked: map = control_failure();
    if map_bool(checked, "ok", false) == false {
        result = fail(types::map_string(checked, "code", "cancelled"), types::map_string(checked, "message", ""), unpublished());
    } else {
        let path_ok: map = validate_tool_path(path, false);
        if map_bool(path_ok, "ok", false) == false {
            result = fail(types::map_string(path_ok, "code", "path_denied"), types::map_string(path_ok, "message", ""), unpublished());
        } else {
            if old_string.length == 0 {
                result = fail("invalid_arguments", "patch old_string must be non-empty", unpublished());
            } else {
                let max_read_bytes: int = map_int(config, "max_read_bytes", 1048576);
                let max_patch_bytes: int = map_int(config, "max_patch_bytes", 8388608);
                let max_patch_preview_bytes: int = map_int(config, "max_patch_preview_bytes", 16384);
                let max_output_bytes: int = map_int(config, "max_tool_output_bytes", 65536);
                let meta: map = cap::fs_metadata(token, path);
                if map_bool(meta, "ok", false) == false {
                    result = fail_host(meta);
                } else {
                    let file_type: string = types::map_string(meta, "file_type", "");
                    if file_type != "file" {
                        result = fail("path_denied", "read-only open requires a regular file", unpublished());
                    } else {
                        let file_len: int = map_int(meta, "len", 0);
                        if file_len > max_read_bytes {
                            result = fail("budget_exceeded", "read budget exceeded", unpublished());
                        } else {
                            let mut source: string = "";
                            let mut expected_hash: string = "";
                            let mut decoded: bool = true;
                            if file_len > 0 {
                                let read: map = cap::fs_read_range(token, path, 0, file_len);
                                if map_bool(read, "ok", false) == false {
                                    result = fail_host(read);
                                    decoded = false;
                                } else {
                                    if map_bool(read, "truncated", false) {
                                        result = fail("budget_exceeded", "read budget exceeded", unpublished());
                                        decoded = false;
                                    } else {
                                        let expected_from_read: string = types::map_string(read, "hash", "");
                                        let payload: bytes = read.bytes;
                                        let data: array = bytes::to_array_u8(payload);
                                        if data.length > max_patch_bytes {
                                            result = fail("patch_too_large", "source exceeds the configured patch budget", unpublished());
                                            decoded = false;
                                        } else {
                                            if bytes_contains_nul(data) {
                                                result = fail("binary_file", "file contains binary content", unpublished());
                                                decoded = false;
                                            } else {
                                                if utf8_is_valid(data) == false {
                                                    result = fail("invalid_utf8", "file is not valid UTF-8", unpublished());
                                                    decoded = false;
                                                } else {
                                                    source = bytes::to_utf8(payload);
                                                    expected_hash = expected_from_read;
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                if 0 > max_patch_bytes {
                                    result = fail("patch_too_large", "source exceeds the configured patch budget", unpublished());
                                    decoded = false;
                                }
                            }
                            if decoded {
                                let counted: map = count_matches(source, old_string);
                                if map_bool(counted, "ok", false) == false {
                                    result = fail(types::map_string(counted, "code", "cancelled"), types::map_string(counted, "message", ""), unpublished());
                                } else {
                                    let matches: int = map_int(counted, "count", 0);
                                    if matches == 0 {
                                        result = fail("patch_no_match", "old_string was not found", unpublished());
                                    } else {
                                        if matches > 1 && replace_all == false {
                                            result = fail("patch_multiple_matches", "old_string matches more than once", { publication: "not_published", matches: matches });
                                        } else {
                                            let mut replacements: int = 1;
                                            if replace_all {
                                                replacements = matches;
                                            }
                                            let mut limit: int = 1;
                                            if replace_all {
                                                limit = matches;
                                            }
                                            let updated: string = replace_text(source, old_string, new_string, limit);
                                            if utf8_len(updated) > max_patch_bytes {
                                                result = fail("patch_too_large", "result exceeds the configured patch budget", unpublished());
                                            } else {
                                                let before_publish: map = control_failure();
                                                if map_bool(before_publish, "ok", false) == false {
                                                    result = fail(types::map_string(before_publish, "code", "cancelled"), types::map_string(before_publish, "message", ""), unpublished());
                                                } else {
                                                    let payload: bytes = bytes::from_utf8(updated);
                                                    let written: map = cap::fs_write_atomic(token, path, expected_hash, payload);
                                                    if map_bool(written, "ok", false) == false {
                                                        result = fail_host(written);
                                                    } else {
                                                        let preview: string = bounded_diff(path, source, updated, max_patch_preview_bytes);
                                                        let mut published: map = succeed(
                                                            preview,
                                                            {
                                                                publication: "published",
                                                                durable: map_bool(written, "durable", true),
                                                                staging_cleaned: map_bool(written, "staging_cleaned", true),
                                                                bytes: map_int(written, "len", utf8_len(updated)),
                                                                replacements: replacements
                                                            },
                                                            false,
                                                            []
                                                        );
                                                        result = shrink_result(published, max_output_bytes, token);
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
    result
}

pub fn run(context: map) -> map {
    let kind: string = types::map_string(context, "kind", "execute");
    if kind == "descriptor" => {
        descriptor()
    } else if kind == "validate" => {
        validate(types::map_map(context, "arguments"))
    } else => {
        let arguments: map = types::map_map(context, "arguments");
        let validated: map = validate(arguments);
        if map_bool(validated, "ok", false) == false => {
            let code: string = types::map_string(validated, "code", "invalid_arguments");
            let message: string = types::map_string(validated, "message", "invalid arguments");
            let mut data: map = unpublished();
            if code == "invalid_arguments" {
                if message != "patch old_string must be non-empty" {
                    data = {};
                }
            }
            fail(code, message, data)
        } else => {
            let prepared: map = agent_runtime::tool_prepare(types::map_map(context, "prepare"));
            if types::map_string(prepared, "kind", "") == "replay" => {
                types::map_map(prepared, "result")
            } else if map_bool(prepared, "ok", false) == false => {
                let error: map = types::map_map(prepared, "error");
                fail(
                    types::map_string(error, "code", "internal_error"),
                    types::map_string(error, "message", "capability failed"),
                    {}
                )
            } else => {
                let token: string = types::map_string(prepared, "execution_token", "");
                let result: map = execute(
                    {
                        execution_token: token,
                        config: types::map_map(context, "config")
                    },
                    arguments
                );
                let committed: map = agent_runtime::tool_commit(token, result);
                if types::map_string(committed, "kind", "") == "committed" => {
                    types::map_map(committed, "result")
                } else if map_bool(committed, "ok", false) == false => {
                    let error: map = types::map_map(committed, "error");
                    fail(
                        types::map_string(error, "code", "internal_error"),
                        types::map_string(error, "message", "capability failed"),
                        {}
                    )
                } else => {
                    result
                }
            }
        }
    }
}
