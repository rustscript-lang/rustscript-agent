use self::types as types;
use bytes;
use json;

fn map_int(value: map, key: string, fallback: int) -> int {
    let mut result: int = fallback;
    if value.has(key) {
        if type(value[key]) == "int" {
            result = value[key];
        }
    }
    result
}

fn map_bool(value: map, key: string, fallback: bool) -> bool {
    let mut result: bool = fallback;
    if value.has(key) {
        if type(value[key]) == "bool" {
            let coerced: bool = value[key];
            result = coerced;
        }
    }
    result
}

fn starts_with(value: string, prefix: string) -> bool {
    let mut result: bool = false;
    if prefix.length <= value.length {
        result = value[0:prefix.length] == prefix;
    }
    result
}

fn char_at(value: string, index: int) -> string {
    value[index:(index + 1)]
}

fn string_contains(haystack: string, needle: string) -> bool {
    let mut found: bool = false;
    if needle.length == 0 {
        found = true;
    } else {
        let mut index: int = 0;
        let limit: int = haystack.length - needle.length + 1;
        while found == false && index < limit {
            if haystack[index:(index + needle.length)] == needle {
                found = true;
            }
            index = index + 1;
        }
    }
    found
}

fn glob_match(pattern: string, text: string) -> bool {
    let pattern_bytes: bytes = bytes::from_utf8(pattern);
    let text_bytes: bytes = bytes::from_utf8(text);
    let pat: array = bytes::to_array_u8(pattern_bytes);
    let data: array = bytes::to_array_u8(text_bytes);
    glob_match_bytes(pat, data)
}

fn glob_match_bytes(pat: array, text: array) -> bool {
    let mut pi: int = 0;
    let mut ti: int = 0;
    let mut star_p: int = -1;
    let mut star_t: int = 0;
    while ti < text.length {
        let mut advanced: bool = false;
        if pi < pat.length {
            let p: int = pat[pi];
            if p != 42 {
                if p == 63 || p == text[ti] {
                    pi = pi + 1;
                    ti = ti + 1;
                    advanced = true;
                }
            }
        }
        if advanced == false {
            if pi < pat.length {
                if pat[pi] == 42 {
                    star_p = pi;
                    pi = pi + 1;
                    star_t = ti;
                    advanced = true;
                }
            }
        }
        if advanced == false {
            if star_p >= 0 {
                pi = star_p + 1;
                star_t = star_t + 1;
                ti = star_t;
                advanced = true;
            }
        }
        if advanced == false {
            ti = text.length;
            pi = -1;
        }
    }
    while pi >= 0 && pi < pat.length {
        if pat[pi] == 42 {
            pi = pi + 1;
        } else {
            pi = -1;
        }
    }
    pi == pat.length
}

fn glob_ok(pattern: string, name: string, child: string) -> bool {
    let mut matched: bool = false;
    if glob_match(pattern, name) {
        matched = true;
    } else {
        if glob_match(pattern, child) {
            matched = true;
        }
    }
    matched
}

fn file_glob_ok(state: map, name: string, child: string) -> bool {
    let mut matched: bool = true;
    if map_bool(state, "file_glob_active", false) {
        matched = glob_ok(types::map_string(state, "file_glob", ""), name, child);
    }
    matched
}

fn split_inclusive_newline(text: string) -> array {
    let mut lines: array = [];
    if text.length > 0 {
        let mut start: int = 0;
        let mut index: int = 0;
        while index < text.length {
            if char_at(text, index) == "\n" {
                lines[lines.length] = text[start:(index + 1)];
                start = index + 1;
            }
            index = index + 1;
        }
        if start < text.length {
            lines[lines.length] = text[start:text.length];
        }
    }
    lines
}

fn trim_crlf(line: string) -> string {
    let mut end: int = line.length;
    let mut walking: bool = true;
    while walking && end > 0 {
        let ch: string = char_at(line, end - 1);
        if ch == "\n" || ch == "\r" {
            end = end - 1;
        } else {
            walking = false;
        }
    }
    line[0:end]
}

fn join_rel(parent: string, name: string) -> string {
    if parent.length == 0 => {
        name
    } else => {
        parent + "/" + name
    }
}

fn join_lines(lines: array) -> string {
    let mut out: string = "";
    let mut index: int = 0;
    while index < lines.length {
        if index > 0 {
            out = out + "\n";
        }
        let line: string = lines[index].copy();
        out = out + line;
        index = index + 1;
    }
    out
}

fn sort_strings(items: array) -> array {
    merge_sort_strings(copy_array(items))
}

fn sort_entries(entries: array) -> array {
    merge_sort_maps(copy_array(entries))
}

fn copy_array(items: array) -> array {
    let mut out: array = [];
    let mut i: int = 0;
    while i < items.length {
        out[out.length] = items[i].copy();
        i = i + 1;
    }
    out
}

fn merge_sort_strings(items: array) -> array {
    let mut current: array = items;
    let mut width: int = 1;
    while width < current.length {
        let mut next: array = [];
        let mut start: int = 0;
        while start < current.length {
            let mut mid: int = start + width;
            if mid > current.length {
                mid = current.length;
            }
            let mut end: int = mid + width;
            if end > current.length {
                end = current.length;
            }
            let left: array = slice_array(current, start, mid);
            let right: array = slice_array(current, mid, end);
            let merged: array = merge_string_arrays(left, right);
            let mut k: int = 0;
            while k < merged.length {
                next[next.length] = merged[k].copy();
                k = k + 1;
            }
            start = start + width + width;
        }
        current = copy_array(next);
        width = width + width;
    }
    current
}

fn merge_sort_maps(items: array) -> array {
    let mut current: array = items;
    let mut width: int = 1;
    while width < current.length {
        let mut next: array = [];
        let mut start: int = 0;
        while start < current.length {
            let mut mid: int = start + width;
            if mid > current.length {
                mid = current.length;
            }
            let mut end: int = mid + width;
            if end > current.length {
                end = current.length;
            }
            let left: array = slice_array(current, start, mid);
            let right: array = slice_array(current, mid, end);
            let merged: array = merge_map_arrays(left, right);
            let mut k: int = 0;
            while k < merged.length {
                next[next.length] = merged[k].copy();
                k = k + 1;
            }
            start = start + width + width;
        }
        current = copy_array(next);
        width = width + width;
    }
    current
}

fn slice_array(items: array, start: int, end: int) -> array {
    let mut out: array = [];
    let mut i: int = start;
    while i < end {
        out[out.length] = items[i].copy();
        i = i + 1;
    }
    out
}

fn merge_string_arrays(left: array, right: array) -> array {
    let mut out: array = [];
    let mut i: int = 0;
    let mut j: int = 0;
    while i < left.length && j < right.length {
        let a: string = left[i].copy();
        let b: string = right[j].copy();
        if a <= b {
            out[out.length] = a;
            i = i + 1;
        } else {
            out[out.length] = b;
            j = j + 1;
        }
    }
    while i < left.length {
        out[out.length] = left[i].copy();
        i = i + 1;
    }
    while j < right.length {
        out[out.length] = right[j].copy();
        j = j + 1;
    }
    out
}

fn merge_map_arrays(left: array, right: array) -> array {
    let mut out: array = [];
    let mut i: int = 0;
    let mut j: int = 0;
    while i < left.length && j < right.length {
        let left_item: map = left[i].copy();
        let right_item: map = right[j].copy();
        let a: string = types::map_string(left_item, "name", "");
        let b: string = types::map_string(right_item, "name", "");
        if a <= b {
            out[out.length] = left_item;
            i = i + 1;
        } else {
            out[out.length] = right_item;
            j = j + 1;
        }
    }
    while i < left.length {
        out[out.length] = left[i].copy();
        i = i + 1;
    }
    while j < right.length {
        out[out.length] = right[j].copy();
        j = j + 1;
    }
    out
}

fn slice_lines(lines: array, offset: int, limit: int) -> array {
    let mut selected: array = [];
    let mut index: int = 0;
    while index < lines.length {
        if index >= offset {
            if selected.length < limit {
                selected[selected.length] = lines[index].copy();
            }
        }
        index = index + 1;
    }
    selected
}

fn bytes_contains_nul(data: array) -> bool {
    let mut found: bool = false;
    let mut index: int = 0;
    while found == false && index < data.length {
        let byte: int = data[index];
        if byte == 0 {
            found = true;
        }
        index = index + 1;
    }
    found
}

fn utf8_is_valid(data: array) -> bool {
    let mut valid: bool = true;
    let mut index: int = 0;
    while valid && index < data.length {
        let b: int = data[index];
        if b < 0 || b > 255 {
            valid = false;
        } else {
            if b <= 127 {
                index = index + 1;
            } else {
                if b >= 194 && b <= 223 {
                    if index + 1 >= data.length {
                        valid = false;
                    } else {
                        let c: int = data[index + 1];
                        if c < 128 || c > 191 {
                            valid = false;
                        } else {
                            index = index + 2;
                        }
                    }
                } else {
                    if b >= 224 && b <= 239 {
                        if index + 2 >= data.length {
                            valid = false;
                        } else {
                            let c: int = data[index + 1];
                            let d: int = data[index + 2];
                            let mut ok: bool = c >= 128 && c <= 191 && d >= 128 && d <= 191;
                            if b == 224 {
                                if c < 160 {
                                    ok = false;
                                }
                            }
                            if b == 237 {
                                if c > 159 {
                                    ok = false;
                                }
                            }
                            if ok {
                                index = index + 3;
                            } else {
                                valid = false;
                            }
                        }
                    } else {
                        if b >= 240 && b <= 244 {
                            if index + 3 >= data.length {
                                valid = false;
                            } else {
                                let c: int = data[index + 1];
                                let d: int = data[index + 2];
                                let e: int = data[index + 3];
                                let mut ok: bool = c >= 128 && c <= 191 && d >= 128 && d <= 191 && e >= 128 && e <= 191;
                                if b == 240 {
                                    if c < 144 {
                                        ok = false;
                                    }
                                }
                                if b == 244 {
                                    if c > 143 {
                                        ok = false;
                                    }
                                }
                                if ok {
                                    index = index + 4;
                                } else {
                                    valid = false;
                                }
                            }
                        } else {
                            valid = false;
                        }
                    }
                }
            }
        }
    }
    valid
}

fn deny_path(message: string) -> map {
    { ok: false, code: "path_denied", message: message }
}

fn ok_path() -> map {
    { ok: true, code: "", message: "" }
}

fn validate_tool_path(path: string, allow_empty: bool) -> map {
    let mut result: map = ok_path();
    if path.length == 0 {
        if allow_empty == false {
            result = deny_path("empty paths are not valid file paths");
        }
    } else {
        if utf8_len(path) > 4096 {
            result = deny_path("relative path exceeds the hard bound");
        }
        if map_bool(result, "ok", false) {
            let mut index: int = 0;
            while map_bool(result, "ok", false) && index < path.length {
                if char_at(path, index) == "\0" {
                    result = deny_path("path contains a NUL byte");
                }
                index = index + 1;
            }
        }
        if map_bool(result, "ok", false) {
            if starts_with(path, "/") {
                result = deny_path("rooted or trailing-separator paths are not permitted");
            }
        }
        if map_bool(result, "ok", false) {
            if char_at(path, path.length - 1) == "/" {
                result = deny_path("rooted or trailing-separator paths are not permitted");
            }
        }
        if map_bool(result, "ok", false) {
            let mut index: int = 0;
            while map_bool(result, "ok", false) && index < path.length {
                if char_at(path, index) == "\\" {
                    result = deny_path("backslash is not a permitted path separator");
                }
                if char_at(path, index) == ":" {
                    result = deny_path("drive and prefix syntax is not permitted");
                }
                index = index + 1;
            }
        }
        if map_bool(result, "ok", false) {
            let mut start: int = 0;
            let mut index: int = 0;
            while map_bool(result, "ok", false) && index <= path.length {
                let at_end: bool = index == path.length;
                let mut is_sep: bool = false;
                if at_end == false {
                    if char_at(path, index) == "/" {
                        is_sep = true;
                    }
                }
                if at_end || is_sep {
                    if index == start {
                        result = deny_path("empty path components are not permitted");
                    } else {
                        let component: string = path[start:index];
                        if component == "." || component == ".." {
                            result = deny_path("dot and parent components are not permitted");
                        }
                        if map_bool(result, "ok", false) {
                            if char_at(component, component.length - 1) == "." {
                                result = deny_path("trailing-dot components are not permitted");
                            }
                        }
                        if map_bool(result, "ok", false) {
                            if utf8_len(component) > 255 {
                                result = deny_path("path component exceeds the hard bound");
                            }
                        }
                        start = index + 1;
                    }
                }
                index = index + 1;
            }
        }
    }
    result
}

fn fail(code: string, message: string, data: map) -> map {
    let mut mapped: string = message;
    if code == "cancelled" {
        mapped = "tool execution was cancelled";
    }
    if code == "deadline_elapsed" {
        mapped = "tool deadline elapsed";
    }
    {
        ok: false,
        content: "",
        data: data,
        error: { code: code, message: mapped },
        truncated: false,
        artifacts: []
    }
}

fn succeed(content: string, data: map, truncated: bool, artifacts: array) -> map {
    {
        ok: true,
        content: content,
        data: data,
        error: null,
        truncated: truncated,
        artifacts: artifacts
    }
}

fn fail_host(envelope: map) -> map {
    let error: map = types::map_map(envelope, "error");
    fail(
        types::map_string(error, "code", "internal_error"),
        types::map_string(error, "message", "capability failed"),
        {}
    )
}

fn encoded_len(value: map) -> int {
    let encoded: string = json::encode(value);
    utf8_len(encoded)
}

fn utf8_len(value: string) -> int {
    let payload: bytes = bytes::from_utf8(value);
    payload.length
}

fn truncate_to_utf8_bytes(text: string, limit: int) -> string {
    let mut end: int = 0;
    let mut used: int = 0;
    let mut walking: bool = true;
    while walking && end < text.length {
        let ch: string = char_at(text, end);
        let ch_bytes: int = utf8_len(ch);
        if used + ch_bytes > limit {
            walking = false;
        } else {
            used = used + ch_bytes;
            end = end + 1;
        }
    }
    text[0:end]
}

fn fit_content_to_budget(result: map, cap: int) -> map {
    let content: string = types::map_string(result, "content", "");
    let mut fitted: map = result;
    fitted.content = "";
    fitted.truncated = true;
    if encoded_len(fitted) <= cap {
        let mut budget: int = cap - encoded_len(fitted);
        let mut walking: bool = true;
        while walking {
            fitted.content = truncate_to_utf8_bytes(content, budget);
            fitted.truncated = true;
            if encoded_len(fitted) <= cap {
                walking = false;
            } else {
                if budget <= 0 {
                    fitted.content = "";
                    walking = false;
                } else {
                    budget = budget / 2;
                }
            }
        }
    }
    fitted
}

fn skip_search_code(code: string) -> bool {
    code == "path_denied" || code == "not_found" || code == "wrong_type" || code == "permission_denied" || code == "budget_exceeded" || code == "invalid_utf8" || code == "invalid_data"
}

fn control_failure() -> map {
    let control: map = agent::control_check();
    if map_bool(control, "ok", false) => {
        { ok: true, code: "", message: "" }
    } else => {
        let error: map = types::map_map(control, "error");
        {
            ok: false,
            code: types::map_string(error, "code", "cancelled"),
            message: types::map_string(error, "message", "cancelled")
        }
    }
}

fn read_text(token: string, path: string, max_read_bytes: int) -> map {
    let mut result: map = { ok: false, skip: false, content_skip: false, text: "", code: "", message: "" };
    let read: map = cap::fs_read_range(token, path, 0, max_read_bytes);
    if map_bool(read, "ok", false) == false {
        let error: map = types::map_map(read, "error");
        let code: string = types::map_string(error, "code", "internal_error");
        if skip_search_code(code) {
            result = { ok: false, skip: true, content_skip: false, text: "", code: code, message: types::map_string(error, "message", "") };
        } else {
            result = { ok: false, skip: false, content_skip: false, text: "", code: code, message: types::map_string(error, "message", "") };
        }
    } else {
        if map_bool(read, "truncated", false) {
            result = { ok: false, skip: true, content_skip: false, text: "", code: "budget_exceeded", message: "read budget exceeded" };
        } else {
            let payload: bytes = read.bytes;
            let data: array = bytes::to_array_u8(payload);
            if bytes_contains_nul(data) {
                result = { ok: true, skip: false, content_skip: true, text: "", code: "invalid_data", message: "binary" };
            } else {
                if utf8_is_valid(data) == false {
                    result = { ok: true, skip: false, content_skip: true, text: "", code: "invalid_utf8", message: "utf8" };
                } else {
                    result = { ok: true, skip: false, content_skip: false, text: bytes::to_utf8(payload), code: "", message: "" };
                }
            }
        }
    }
    result
}

fn push_match(state: map, line: string) -> map {
    let max_matches: int = map_int(state, "max_search_matches", 200);
    let max_output_bytes: int = map_int(state, "max_search_output_bytes", 65536);
    let mut lines: array = types::map_array(state, "lines");
    let mut extra: int = 0;
    if lines.length > 0 {
        extra = 1;
    }
    if lines.length >= max_matches {
        state.truncated = true;
        state.stop = true;
    } else {
        if extra + utf8_len(line) + map_int(state, "match_bytes", 0) > max_output_bytes {
            state.truncated = true;
            state.stop = true;
        } else {
            lines[lines.length] = line;
            state.lines = lines;
            state.match_bytes = map_int(state, "match_bytes", 0) + extra + utf8_len(line);
            if lines.length >= max_matches {
                state.truncated = true;
                state.stop = true;
            }
        }
    }
    state
}

fn observe_limits(state: map) -> map {
    let tick: map = cap::clock_monotonic_ms(types::map_string(state, "token", ""));
    if map_bool(tick, "ok", false) == false {
        state.fatal = true;
        state.fatal_code = types::map_string(types::map_map(tick, "error"), "code", "internal_error");
        state.fatal_message = types::map_string(types::map_map(tick, "error"), "message", "capability failed");
        state.stop = true;
    } else {
        let now: int = map_int(tick, "ms", 0);
        let start: int = map_int(state, "start_ms", 0);
        if now < start {
            state.truncated = true;
            state.stop = true;
        } else {
            let elapsed: int = now - start;
            if elapsed >= map_int(state, "max_search_wall_time_ms", 2000) {
                state.truncated = true;
                state.stop = true;
            }
        }
    }
    if map_int(state, "files_visited", 0) >= map_int(state, "max_search_files", 10000) {
        state.truncated = true;
        state.stop = true;
    }
    if map_int(state, "scanned_bytes", 0) >= map_int(state, "max_search_scanned_bytes", 16777216) {
        state.truncated = true;
        state.stop = true;
    }
    let checked: map = control_failure();
    if map_bool(checked, "ok", false) == false {
        state.fatal = true;
        state.fatal_code = types::map_string(checked, "code", "cancelled");
        state.fatal_message = types::map_string(checked, "message", "");
        state.stop = true;
    }
    state
}

fn walk_search(state: map, path: string, depth: int) -> map {
    state.dirs_visited = map_int(state, "dirs_visited", 0) + 1;
    state = observe_limits(state);
    if map_bool(state, "stop", false) == false {
        if depth > map_int(state, "max_search_depth", 32) {
            state.truncated = true;
        } else {
            let remaining: int = map_int(state, "max_search_files", 10000) - map_int(state, "files_visited", 0);
            if remaining <= 0 {
                state.truncated = true;
                state.stop = true;
            } else {
                if remaining <= 1 {
                    state.truncated = true;
                    state.stop = true;
                } else {
                    let mut list_limit: int = remaining - 2;
                    if list_limit <= 0 {
                        list_limit = 1;
                    }
                    let listed: map = cap::fs_list(types::map_string(state, "token", ""), path, 0, list_limit);
                if map_bool(listed, "ok", false) == false {
                    let error: map = types::map_map(listed, "error");
                    let code: string = types::map_string(error, "code", "internal_error");
                    if code == "budget_exceeded" {
                        state.truncated = true;
                        state.stop = true;
                    } else {
                        let mut mapped_code: string = code;
                        let mut mapped_message: string = types::map_string(error, "message", "");
                        if mapped_message == "symlinks are not followed" {
                            mapped_code = "wrong_type";
                            mapped_message = "operating-system operation failed";
                        } else {
                            if code == "wrong_type" {
                                mapped_message = "operating-system operation failed";
                            }
                        }
                        state.fatal = true;
                        state.fatal_code = mapped_code;
                        state.fatal_message = mapped_message;
                        state.stop = true;
                    }
                } else {
                    let mut drop_page: bool = false;
                    if remaining <= 2 {
                        let probe_entries: array = types::map_array(listed, "entries");
                        if map_bool(listed, "truncated", false) {
                            drop_page = true;
                        } else {
                            if probe_entries.length > 0 {
                                drop_page = true;
                            }
                        }
                    } else {
                        if map_bool(listed, "truncated", false) {
                            drop_page = true;
                        }
                    }
                    if drop_page {
                        state.truncated = true;
                        state.stop = true;
                    } else {
                    let mut entries: array = types::map_array(listed, "entries");
                    entries = sort_entries(entries);
                    let mut index: int = 0;
                    while map_bool(state, "stop", false) == false && index < entries.length {
                        let entry: map = entries[index].copy();
                        let name: string = types::map_string(entry, "name", "");
                        if starts_with(name, ".rustscript-agent-tmp-") == false {
                            let child: string = join_rel(path, name);
                            let file_type: string = types::map_string(entry, "file_type", "");
                            if file_type == "directory" {
                                state = walk_search(state, child, depth + 1);
                            } else {
                                if file_type == "file" {
                                    state = observe_limits(state);
                                    if map_bool(state, "stop", false) == false {
                                        state.files_visited = map_int(state, "files_visited", 0) + 1;
                                        if file_glob_ok(state, name, child) {
                                            let token: string = types::map_string(state, "token", "");
                                            let pattern: string = types::map_string(state, "pattern", "");
                                            if map_bool(state, "target_files", false) {
                                                if glob_ok(pattern, name, child) {
                                                    state = push_match(state, child);
                                                }
                                            } else {
                                                let len: int = map_int(entry, "len", 0);
                                                let remaining_scan: int = map_int(state, "max_search_scanned_bytes", 16777216) - map_int(state, "scanned_bytes", 0);
                                                if len > remaining_scan {
                                                    state.truncated = true;
                                                    state.stop = true;
                                                } else {
                                                    let loaded: map = read_text(token, child, map_int(state, "max_read_bytes", 1048576));
                                                    if map_bool(loaded, "skip", false) == false {
                                                        if map_bool(loaded, "ok", false) == false {
                                                            state.fatal = true;
                                                            state.fatal_code = types::map_string(loaded, "code", "internal_error");
                                                            state.fatal_message = types::map_string(loaded, "message", "");
                                                            state.stop = true;
                                                        } else {
                                                            state.scanned_bytes = map_int(state, "scanned_bytes", 0) + len;
                                                            if map_bool(loaded, "content_skip", false) == false {
                                                                let text: string = types::map_string(loaded, "text", "");
                                                                let lines: array = split_inclusive_newline(text);
                                                                let mut line_index: int = 0;
                                                                while map_bool(state, "stop", false) == false && line_index < lines.length {
                                                                    state = observe_limits(state);
                                                                    if map_bool(state, "stop", false) == false {
                                                                        let line: string = lines[line_index].copy();
                                                                        if string_contains(line, pattern) {
                                                                            let trimmed: string = trim_crlf(line);
                                                                            let encoded: string = json::encode(line_index + 1);
                                                                            state = push_match(state, child + ":" + encoded + ":" + trimmed);
                                                                        }
                                                                    }
                                                                    line_index = line_index + 1;
                                                                }
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                        index = index + 1;
                    }
                    }
                }
            }
            }
        }
    }
    state
}

fn shrink_result(result: map, max_output_bytes: int, token: string) -> map {
    let mut current: map = result;
    if encoded_len(current) > max_output_bytes {
        let content: string = types::map_string(current, "content", "");
        let data: map = types::map_map(current, "data");
        if content.length > 0 {
            let payload: bytes = bytes::from_utf8(content);
            let put: map = cap::artifact_put_result(token, payload, {});
            if map_bool(put, "ok", false) {
                let id: string = types::map_string(put, "id", "");
                let len: int = map_int(put, "len", utf8_len(content));
                let mut artifacts: array = [];
                artifacts[0] = id;
                let mut summary: string = "artifact " + id + " (" + json::encode(len) + " bytes)";
                current = succeed(summary, data, true, artifacts);
                if encoded_len(current) > max_output_bytes {
                    current = succeed("artifact " + id, data, true, artifacts);
                }
                if encoded_len(current) > max_output_bytes {
                    current = succeed("artifact", data, true, artifacts);
                }
            }
        }
        if encoded_len(current) > max_output_bytes {
            current = fit_content_to_budget(current, max_output_bytes);
        }
        if encoded_len(current) > max_output_bytes {
            current = fail("output_truncated", "tool result exceeds the configured bound", {});
            current.truncated = true;
            if encoded_len(current) > max_output_bytes {
                current = fail("output_truncated", "bounded", {});
                current.truncated = true;
                if encoded_len(current) > max_output_bytes {
                    current = fail("output_truncated", "", {});
                    current.truncated = true;
                }
            }
        }
    }
    current
}

pub fn descriptor() -> map {
    types::search_files_descriptor()
}

pub fn validate(arguments: map) -> map {
    let mut result: map = { ok: true, code: "", message: "" };
    if arguments.has("pattern") == false {
        result = { ok: false, code: "invalid_arguments", message: "search_files requires pattern" };
    } else {
        if type(arguments.pattern) != "string" {
            result = { ok: false, code: "invalid_arguments", message: "search_files requires pattern" };
        } else {
            let pattern: string = arguments.pattern;
            if pattern.length == 0 {
                result = { ok: false, code: "invalid_arguments", message: "search_files requires a pattern" };
            }
        }
    }
    if map_bool(result, "ok", false) {
        if arguments.has("path") {
            if type(arguments.path) == "string" {
                result = validate_tool_path(arguments.path, true);
            }
        }
    }
    if map_bool(result, "ok", false) {
        if arguments.has("offset") {
            if type(arguments.offset) != "int" {
                result = { ok: false, code: "invalid_arguments", message: "numeric argument is invalid" };
            } else {
                if arguments.offset < 0 {
                    result = { ok: false, code: "invalid_arguments", message: "numeric argument is invalid" };
                }
            }
        }
    }
    if map_bool(result, "ok", false) {
        if arguments.has("limit") {
            if type(arguments.limit) != "int" {
                result = { ok: false, code: "invalid_arguments", message: "numeric argument is invalid" };
            } else {
                if arguments.limit < 0 {
                    result = { ok: false, code: "invalid_arguments", message: "numeric argument is invalid" };
                }
            }
        }
    }
    result
}

pub fn execute(context: map, arguments: map) -> map {
    let token: string = types::map_string(context, "execution_token", "");
    let config: map = types::map_map(context, "config");
    let pattern: string = types::map_string(arguments, "pattern", "");
    let path: string = types::map_string(arguments, "path", "");
    let target: string = types::map_string(arguments, "target", "");
    let mut result: map = { ok: true, content: "", data: {}, error: null, truncated: false, artifacts: [] };
    let checked: map = control_failure();
    if map_bool(checked, "ok", false) == false {
        result = fail(types::map_string(checked, "code", "cancelled"), types::map_string(checked, "message", ""), {});
    } else {
        let path_ok: map = validate_tool_path(path, true);
        if map_bool(path_ok, "ok", false) == false {
            result = fail(types::map_string(path_ok, "code", "path_denied"), types::map_string(path_ok, "message", ""), {});
        } else {
            let mut file_glob: string = "";
            let mut file_glob_active: bool = false;
            if arguments.has("file_glob") {
                if type(arguments.file_glob) == "string" {
                    file_glob = arguments.file_glob;
                    file_glob_active = true;
                }
            }
            let mut offset: int = 0;
            if arguments.has("offset") {
                offset = arguments.offset;
            }
            let mut limit: int = map_int(config, "max_search_matches", 10000);
            if arguments.has("limit") {
                limit = arguments.limit;
            }
            if limit > map_int(config, "max_search_matches", 10000) {
                limit = map_int(config, "max_search_matches", 10000);
            }
            let mut state: map = {
                token: token,
                pattern: pattern,
                file_glob: file_glob,
                file_glob_active: file_glob_active,
                target_files: target == "files",
                lines: [],
                files_visited: 0,
                dirs_visited: 0,
                scanned_bytes: 0,
                match_bytes: 0,
                truncated: false,
                stop: false,
                fatal: false,
                fatal_code: "",
                fatal_message: "",
                start_ms: 0,
                max_search_files: map_int(config, "max_search_files", 10000),
                max_search_scanned_bytes: map_int(config, "max_search_scanned_bytes", 16777216),
                max_search_depth: map_int(config, "max_search_depth", 32),
                max_search_matches: map_int(config, "max_search_matches", 10000),
                max_search_output_bytes: map_int(config, "max_search_output_bytes", 65536),
                max_search_wall_time_ms: map_int(config, "max_search_wall_time_ms", 2000),
                max_read_bytes: map_int(config, "max_read_bytes", 1048576)
            };
            let start_clock: map = cap::clock_monotonic_ms(token);
            if map_bool(start_clock, "ok", false) == false {
                result = fail_host(start_clock);
            } else {
                state.start_ms = map_int(start_clock, "ms", 0);
                state = observe_limits(state);
                if map_bool(state, "stop", false) == false {
                    state = walk_search(state, path, 0);
                }
                if map_bool(state, "fatal", false) {
                    result = fail(types::map_string(state, "fatal_code", "internal_error"), types::map_string(state, "fatal_message", ""), {});
                } else {
                    let collected: array = types::map_array(state, "lines");
                    let sorted: array = sort_strings(collected);
                    let selected: array = slice_lines(sorted, offset, limit);
                    result = shrink_result(
                        succeed(
                            join_lines(selected),
                            {
                                match_count: selected.length,
                                files_visited: map_int(state, "files_visited", 0),
                                dirs_visited: map_int(state, "dirs_visited", 0)
                            },
                            map_bool(state, "truncated", false),
                            []
                        ),
                        map_int(config, "max_tool_output_bytes", 65536),
                        token
                    );
                }
            }
        }
    }
    result
}

pub fn run(context: map) -> map {
    let kind: string = types::map_string(context, "kind", "execute");
    if kind == "descriptor" => {
        descriptor()
    } else if kind == "validate" => {
        validate(types::map_map(context, "arguments"))
    } else => {
        let arguments: map = types::map_map(context, "arguments");
        let validated: map = validate(arguments);
        if map_bool(validated, "ok", false) == false => {
            fail(
                types::map_string(validated, "code", "invalid_arguments"),
                types::map_string(validated, "message", "invalid arguments"),
                {}
            )
        } else => {
            let prepared: map = agent_runtime::tool_prepare(types::map_map(context, "prepare"));
            if types::map_string(prepared, "kind", "") == "replay" => {
                types::map_map(prepared, "result")
            } else if map_bool(prepared, "ok", false) == false => {
                fail_host(prepared)
            } else => {
                let token: string = types::map_string(prepared, "execution_token", "");
                let result: map = execute(
                    {
                        execution_token: token,
                        config: types::map_map(context, "config")
                    },
                    arguments
                );
                let committed: map = agent_runtime::tool_commit(token, result);
                if types::map_string(committed, "kind", "") == "committed" => {
                    types::map_map(committed, "result")
                } else if map_bool(committed, "ok", false) == false => {
                    fail_host(committed)
                } else => {
                    result
                }
            }
        }
    }
}
