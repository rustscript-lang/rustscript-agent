// Atomic run admission: one transaction commits the session (created or
// touched), the first user message, the run, the parent/child link, the
// idempotency record, the `run.started` event, and the retention floor in a
// single durable commit. A failure leaves no partial admission state on
// disk, so a restart can never resurrect a half-admitted run.
use json;
use sqlite;
use self::existence as existence;

struct AdmissionCreateInput {
    session_id: string,
    session_new: int,
    profile: string,
    platform: string,
    account_id: string,
    model: string,
    provider: string,
    system_prompt: string,
    run_id: string,
    parent_run_id: string,
    input_json: string,
    message_id: string,
    message_run_id: string,
    script_hash: string,
    idempotency_scope: string,
    idempotency_key: string,
    request_hash: string,
    event_id: string,
    now_ms: int,
    expires_at_ms: int
}

pub fn storage_admission_create(db_id: resource<sqlite.connection>, payload_json: string) -> map {
    let input: AdmissionCreateInput = json::decode::<AdmissionCreateInput>(payload_json);
    let mut failed: bool = false;
    let mut failure_code: string = "";
    let mut failure_message: string = "";
    if input.session_new.copy() == 0 {
        if !existence::session_exists(db_id, input.session_id.copy()) {
            failed = true;
            failure_code = "session_not_found";
            failure_message = "admission session does not exist";
        }
    }
    if failed == false {
        if input.parent_run_id.copy() != "" && !existence::run_exists(db_id, input.parent_run_id.copy()) {
            failed = true;
            failure_code = "parent_not_found";
            failure_message = "parent run does not exist";
        }
    }
    let mut replay: bool = false;
    let mut conflict: bool = false;
    let mut existing_run_id: string = "";
    if failed == false {
        if input.idempotency_key.copy() != "" {
            let existing: map = sqlite::query(
                &db_id,
                "SELECT scope, key, request_hash, resource_type, resource_id, state, response_json FROM idempotency_records WHERE scope = ? AND key = ? LIMIT 1",
                [&input.idempotency_scope, &input.idempotency_key],
                { max_rows: 1, max_result_bytes: 8192 }
            );
            let rows: array = existing["rows"];
            if rows.length > 0 {
                let row: array = rows[0];
                let existing_hash: string = row[2];
                let existing_resource: string = row[4];
                existing_run_id = existing_resource;
                if existing_hash == input.request_hash.copy() {
                    replay = true;
                } else {
                    conflict = true;
                }
            }
        }
    }
    if failed == false {
        if replay == false && conflict == false {
            let mut statements = [];
            if input.session_new.copy() == 1 {
                statements[statements.length] = {
                    sql: "INSERT INTO sessions (id, profile, platform, account_id, chat_id, thread_id, user_id, generation, system_prompt, model, provider, toolset_hash, metadata_json, created_at_ms, updated_at_ms) SELECT ?, ?, ?, ?, '', '', '', 1, ?, ?, ?, '', '{}', ?, ? WHERE NOT EXISTS (SELECT 1 FROM sessions existing WHERE existing.id = ?)",
                    params: [&input.session_id, &input.profile, &input.platform, &input.account_id, &input.system_prompt, &input.model, &input.provider, input.now_ms.copy(), input.now_ms.copy(), &input.session_id]
                };
            } else {
                statements[statements.length] = {
                    sql: "UPDATE sessions SET model = CASE WHEN ? = '' THEN model ELSE ? END, provider = CASE WHEN ? = '' THEN provider ELSE ? END, system_prompt = CASE WHEN ? = '' THEN system_prompt ELSE ? END, updated_at_ms = ? WHERE id = ?",
                    params: [&input.model, &input.model, &input.provider, &input.provider, &input.system_prompt, &input.system_prompt, input.now_ms.copy(), &input.session_id]
                };
            }
            statements[statements.length] = {
                sql: "INSERT INTO messages (id, session_id, ordinal, role, content_json, metadata_json, run_id, finish_reason, created_at_ms) SELECT ?, ?, COALESCE(MAX(ordinal), 0) + 1, 'user', ?, '{}', ?, '', ? FROM messages WHERE session_id = ? AND NOT EXISTS (SELECT 1 FROM messages existing WHERE existing.id = ? AND existing.session_id <> ?)",
                params: [&input.message_id, &input.session_id, &input.input_json, &input.message_run_id, input.now_ms.copy(), &input.session_id, &input.message_id, &input.session_id]
            };
            statements[statements.length] = {
                sql: "INSERT INTO runs (id, session_id, parent_run_id, status, input_json, provider, model, script_hash, idempotency_scope, idempotency_key, created_at_ms, started_at_ms, updated_at_ms) SELECT ?, ?, ?, 'running', ?, ?, ?, ?, ?, ?, ?, ?, ? WHERE NOT EXISTS (SELECT 1 FROM runs existing WHERE existing.id = ? AND existing.session_id <> ?) AND EXISTS (SELECT 1 FROM sessions WHERE id = ?) AND (? = '' OR EXISTS (SELECT 1 FROM runs parent WHERE parent.id = ?))",
                params: [&input.run_id, &input.session_id, &input.parent_run_id, &input.input_json, &input.provider, &input.model, &input.script_hash, &input.idempotency_scope, &input.idempotency_key, input.now_ms.copy(), input.now_ms.copy(), input.now_ms.copy(), &input.run_id, &input.session_id, &input.session_id, &input.parent_run_id, &input.parent_run_id]
            };
            if input.parent_run_id.copy() != "" {
                statements[statements.length] = {
                    sql: "INSERT INTO child_run_links (parent_run_id, child_run_id, ordinal, relation, state, created_at_ms) SELECT ?, ?, COALESCE(MAX(ordinal), -1) + 1, 'subagent', 'pending', ? FROM child_run_links WHERE parent_run_id = ?",
                    params: [&input.parent_run_id, &input.run_id, input.now_ms.copy(), &input.parent_run_id]
                };
            }
            if input.idempotency_key.copy() != "" {
                statements[statements.length] = {
                    sql: "INSERT OR IGNORE INTO idempotency_records (scope, key, request_hash, resource_type, resource_id, state, response_json, created_at_ms, expires_at_ms) SELECT ?, ?, ?, 'run', ?, 'completed', '{\"run_id\":\"' || ? || '\",\"status\":\"running\"}', ?, ? WHERE ? <> ''",
                    params: [&input.idempotency_scope, &input.idempotency_key, &input.request_hash, &input.run_id, &input.run_id, input.now_ms.copy(), input.expires_at_ms.copy(), &input.idempotency_key]
                };
            }
            statements[statements.length] = {
                sql: "INSERT INTO run_events (run_id, seq, event_id, event_type, payload_json, created_at_ms) SELECT ?, COALESCE((SELECT MAX(events.seq) FROM run_events events WHERE events.run_id = ?), 0) + 1, ?, 'run.started', '{\"status\":\"running\",\"session_id\":\"' || ? || '\"}', ? WHERE EXISTS (SELECT 1 FROM runs WHERE id = ?)",
                params: [&input.run_id, &input.run_id, &input.event_id, &input.session_id, input.now_ms.copy(), &input.run_id]
            };
            statements[statements.length] = {
                sql: "INSERT INTO run_retention (run_id, first_seq, high_water_seq, updated_at_ms) SELECT ?, COALESCE((SELECT MIN(seq) FROM run_events WHERE run_id = ?), 0), COALESCE((SELECT MAX(seq) FROM run_events WHERE run_id = ?), 0), ? WHERE EXISTS (SELECT 1 FROM runs WHERE id = ?) ON CONFLICT (run_id) DO UPDATE SET first_seq = excluded.first_seq, high_water_seq = MAX(run_retention.high_water_seq, excluded.high_water_seq), updated_at_ms = excluded.updated_at_ms",
                params: [&input.run_id, &input.run_id, &input.run_id, input.now_ms.copy(), &input.run_id]
            };
            sqlite::transaction(&db_id, statements);
        }
    }
    let query_run_id: string = if replay => { existing_run_id } else => { input.run_id.copy() };
    let run_result: map = sqlite::query(
        &db_id,
        "SELECT id, session_id, parent_run_id, status, input_json, provider, model, script_hash, idempotency_scope, idempotency_key, turn_count, input_tokens, output_tokens, error_code, error_message, recovery_reason, created_at_ms, started_at_ms, finished_at_ms, updated_at_ms FROM runs WHERE id = ? LIMIT 1",
        [&query_run_id],
        { max_rows: 1, max_result_bytes: 65536 }
    );
    let run_result_copy: map = run_result.copy();
    let run_rows: array = run_result["rows"];
    if failed == false {
        if run_rows.length == 0 && replay == false {
            failed = true;
            failure_code = "session_not_found";
            failure_message = "admission could not create the run";
        }
        if run_rows.length == 0 && replay == true {
            failed = true;
            failure_code = "idempotency_conflict";
            failure_message = "replayed idempotency key references a missing run";
        }
    }
    let session_result: map = sqlite::query(
        &db_id,
        "SELECT id, profile, platform, account_id, chat_id, thread_id, user_id, generation, status, system_prompt, model, provider, toolset_hash, metadata_json, last_message_seq, created_at_ms, updated_at_ms FROM sessions WHERE id = ? LIMIT 1",
        [&input.session_id],
        { max_rows: 1, max_result_bytes: 65536 }
    );
    let message_result: map = sqlite::query(
        &db_id,
        "SELECT id, session_id, ordinal, role, content_json, name, tool_call_id, parent_message_id, token_estimate, compacted, metadata_json, run_id, finish_reason, created_at_ms FROM messages WHERE id = ? AND session_id = ? LIMIT 1",
        [&input.message_id, &input.session_id],
        { max_rows: 1, max_result_bytes: 65536 }
    );
    let idempotency_result: map = if input.idempotency_key.copy() == "" => {
        { columns: [], rows: [] }
    } else => {
        sqlite::query(
            &db_id,
            "SELECT scope, key, request_hash, resource_type, resource_id, state, response_json, created_at_ms, expires_at_ms, completed_at_ms FROM idempotency_records WHERE scope = ? AND key = ? LIMIT 1",
            [&input.idempotency_scope, &input.idempotency_key],
            { max_rows: 1, max_result_bytes: 8192 }
        )
    };
    let mut result = {
        ok: true,
        code: "ok",
        message: "",
        result: {
            replayed: replay,
            conflict: conflict,
            run: run_result_copy,
            session: session_result,
            message: message_result,
            idempotency: idempotency_result
        }
    };
    if conflict == true {
        result = {
            ok: false,
            code: "idempotency_key_conflict",
            message: "idempotency key was used with a different request",
            result: {
                replayed: false,
                conflict: conflict,
                run: run_result_copy,
                session: session_result,
                message: message_result,
                idempotency: idempotency_result
            }
        };
    } else {
        if failed == true {
            result = {
                ok: false,
                code: failure_code,
                message: failure_message,
                result: {
                    replayed: replay,
                    conflict: conflict,
                    run: run_result_copy,
                    session: session_result,
                    message: message_result,
                    idempotency: idempotency_result
                }
            };
        }
    }
    result
}
