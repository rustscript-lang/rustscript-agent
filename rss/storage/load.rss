// Full-state load for restart recovery. Every table is read in bounded
// cursor pages (`rowid > last ORDER BY rowid`), so a load larger than any
// single page or byte limit is never silently truncated: the loop drains
// every page and a hard cap turns an absurd state into a typed error.
//
// Column orders are part of the command contract (mirrored by the Rust
// loaders); the `_cursor` rowid column is stripped from every row.
use json;
use sqlite;

/// The `load.all` command payload: page bounds and the hard total-row cap.
/// `load_cap` is what turns an absurd state into the typed `load_too_large`
/// error (the production gateway sends 1,000,000); tests parameterize it.
struct LoadAllInput {
    max_rows: int,
    max_bytes: int,
    load_cap: int
}

pub fn storage_load_all(db_id: int, payload_json: string) -> map {
    let input: LoadAllInput = json::decode::<LoadAllInput>(payload_json);
    let page_size: int = 512;
    let load_cap: int = input.load_cap.copy();
    let sessions = load_page(
        db_id,
        "SELECT rowid AS _cursor, id, profile, platform, account_id, chat_id, thread_id, user_id, generation, status, system_prompt, model, provider, toolset_hash, metadata_json, last_message_seq, created_at_ms, updated_at_ms, title, end_reason FROM sessions WHERE rowid > ? ORDER BY rowid ASC LIMIT ?",
        page_size,
        load_cap
    );
    let runs = load_page(
        db_id,
        "SELECT rowid AS _cursor, id, session_id, parent_run_id, status, input_json, provider, model, script_hash, idempotency_scope, idempotency_key, turn_count, input_tokens, output_tokens, error_code, error_message, recovery_reason, created_at_ms, started_at_ms, finished_at_ms, updated_at_ms FROM runs WHERE rowid > ? ORDER BY rowid ASC LIMIT ?",
        page_size,
        load_cap
    );
    let jobs = load_page(
        db_id,
        "SELECT rowid AS _cursor, id, name, schedule_json, prompt, deliver_json, skills_json, repeat_count, enabled, output_json, created_at_ms, updated_at_ms, last_run_at_ms FROM jobs WHERE rowid > ? ORDER BY rowid ASC LIMIT ?",
        page_size,
        load_cap
    );
    let idempotency = load_page(
        db_id,
        "SELECT rowid AS _cursor, scope, key, request_hash, resource_type, resource_id, state, response_json, created_at_ms, expires_at_ms, completed_at_ms FROM idempotency_records WHERE rowid > ? ORDER BY rowid ASC LIMIT ?",
        page_size,
        load_cap
    );
    let mut failed: bool = false;
    let mut failure_message: string = "";
    let sessions_overload: bool = sessions["overload"];
    let runs_overload: bool = runs["overload"];
    let jobs_overload: bool = jobs["overload"];
    let idempotency_overload: bool = idempotency["overload"];
    if sessions_overload == true {
        failed = true;
        failure_message = "gateway session state exceeds the load cap";
    }
    if failed == false {
        if runs_overload == true {
            failed = true;
            failure_message = "gateway run state exceeds the load cap";
        }
    }
    if failed == false {
        if jobs_overload == true {
            failed = true;
            failure_message = "gateway job state exceeds the load cap";
        }
    }
    if failed == false {
        if idempotency_overload == true {
            failed = true;
            failure_message = "gateway idempotency state exceeds the load cap";
        }
    }
    let sessions_rows: array = sessions["rows"];
    let runs_rows: array = runs["rows"];
    let jobs_rows: array = jobs["rows"];
    let idempotency_rows: array = idempotency["rows"];
    let mut messages = [];
    if failed == false {
        let session_rows: array = sessions_rows;
        let session_count: int = session_rows.length;
        let mut session_index = 0;
        while session_index < session_count {
            let session_row: array = session_rows[session_index].copy();
            let session_id: string = session_row[0];
            let mut ordinal_cursor: int = 0;
            let mut page_done = false;
            while page_done == false {
                let page: map = sqlite::query(
                    db_id,
                    "SELECT ordinal, id, session_id, role, content_json, name, tool_call_id, parent_message_id, token_estimate, compacted, metadata_json, run_id, finish_reason, created_at_ms FROM messages WHERE session_id = ? AND ordinal > ? ORDER BY ordinal ASC LIMIT ?",
                    [&session_id, ordinal_cursor, page_size + 1],
                    { max_rows: page_size, max_result_bytes: 2097152 }
                );
                let page_rows: array = page["rows"];
                let truncated: bool = page["truncated"];
                let page_count: int = page_rows.length;
                if page_count > 0 {
                    let last_row: array = page_rows[page_count - 1].copy();
                    let last_cursor: int = last_row[0];
                    ordinal_cursor = last_cursor;
                }
                let mut row_index = 0;
                while row_index < page_count {
                    messages[messages.length] = page_rows[row_index].copy();
                    row_index += 1;
                }
                if truncated == false {
                    page_done = true;
                }
                if messages.length > load_cap {
                    failed = true;
                    failure_message = "gateway message state exceeds the load cap";
                    page_done = true;
                }
            }
            if failed == true {
                break;
            }
            session_index += 1;
        }
    }
    let mut events = [];
    if failed == false {
        let run_rows: array = runs_rows;
        let run_count: int = run_rows.length;
        let mut run_index = 0;
        while run_index < run_count {
            let run_row: array = run_rows[run_index].copy();
            let run_id: string = run_row[0];
            let mut seq_cursor: int = 0;
            let mut event_page_done = false;
            while event_page_done == false {
                let page: map = sqlite::query(
                    db_id,
                    "SELECT seq, run_id, event_id, event_type, payload_json, created_at_ms FROM run_events WHERE run_id = ? AND seq > ? ORDER BY seq ASC LIMIT ?",
                    [&run_id, seq_cursor, page_size + 1],
                    { max_rows: page_size, max_result_bytes: 2097152 }
                );
                let page_rows: array = page["rows"];
                let truncated: bool = page["truncated"];
                let page_count: int = page_rows.length;
                if page_count > 0 {
                    let last_row: array = page_rows[page_count - 1].copy();
                    let last_cursor: int = last_row[0];
                    seq_cursor = last_cursor;
                }
                let mut row_index = 0;
                while row_index < page_count {
                    events[events.length] = page_rows[row_index].copy();
                    row_index += 1;
                }
                if truncated == false {
                    event_page_done = true;
                }
                if events.length > load_cap {
                    failed = true;
                    failure_message = "gateway event state exceeds the load cap";
                    event_page_done = true;
                }
            }
            if failed == true {
                break;
            }
            run_index += 1;
        }
    }
    let mut result = {
        ok: true,
        code: "ok",
        message: "",
        result: {
            sessions: sessions_rows,
            messages: messages,
            runs: runs_rows,
            events: events,
            jobs: jobs_rows,
            idempotency: idempotency_rows
        }
    };
    if failed == true {
        result = {
            ok: false,
            code: "load_too_large",
            message: failure_message,
            result: {
                sessions: sessions_rows,
                messages: messages,
                runs: runs_rows,
                events: events,
                jobs: jobs_rows,
                idempotency: idempotency_rows
            }
        };
    }
    result
}

fn load_page(db_id: int, sql: string, page_size: int, load_cap: int) -> map {
    let mut rows = [];
    let mut columns = [];
    let mut last_rowid: int = 0;
    let mut done = false;
    let mut first = true;
    let mut overload: bool = false;
    while done == false {
        // Fetch page_size + 1 rows: the SQL LIMIT alone would exhaust
        // exactly at the host's max_rows cap without setting `truncated`,
        // silently losing the rest of the table.
        let result: map = sqlite::query(
            db_id,
            sql,
            [last_rowid, page_size + 1],
            { max_rows: page_size, max_result_bytes: 2097152 }
        );
        if first {
            let cols: array = result["columns"];
            columns = drop_first_column(cols);
            first = false;
        }
        let page: array = result["rows"];
        let truncated: bool = result["truncated"];
        let page_count: int = page.length;
        if page_count > 0 {
            let last_row: array = page[page_count - 1].copy();
            let last_cursor: int = last_row[0];
            last_rowid = last_cursor;
            let mut index = 0;
            while index < page_count {
                rows[rows.length] = drop_first_column(page[index].copy());
                index += 1;
            }
        }
        if truncated == false {
            done = true;
        }
        if rows.length > load_cap {
            overload = true;
            done = true;
        }
    }
    { columns: columns, rows: rows, overload: overload }
}

fn drop_first_column(row: array) -> array {
    let mut out = [];
    let mut index = 1;
    while index < row.length {
        out[out.length] = row[index];
        index += 1;
    }
    out
}
