// Serial provider/tool agent loop.
//
// `run(context)` drives canonical LlmRequest construction, the bounded native
// host provider bridge, serial tool dispatch, and retry/backoff until a typed
// terminal decision. Follow-up assistant `tool_call` parts use `arguments_json`
// strings; tool results stay user-role `tool_result` parts so adapters see one
// contract. Parallel/task execution is rejected. Provider/network errors
// consume the retry budget; completed tool effects are never retried.
// Durability of messages/events is left to Task 7.

use agent;
use json;

// ---------------------------------------------------------------------------
// Typed context accessors (same-module; defensive dynamic navigation)
// ---------------------------------------------------------------------------

fn ctx_string(value: map, key: string, fallback: string) -> string {
    let mut result: string = fallback;
    if value.has(key) {
        if type(value[key]) == "string" {
            let coerced: string = value[key];
            result = coerced;
        }
    }
    result
}

fn ctx_int(value: map, key: string, fallback: int) -> int {
    let mut result: int = fallback;
    if value.has(key) {
        if type(value[key]) == "int" {
            let coerced: int = value[key];
            result = coerced;
        }
    }
    result
}

fn ctx_bool(value: map, key: string, fallback: bool) -> bool {
    let mut result: bool = fallback;
    if value.has(key) {
        if type(value[key]) == "bool" {
            let coerced: bool = value[key];
            result = coerced;
        }
    }
    result
}

fn ctx_map(value: map, key: string) -> map {
    let mut result: map = {};
    if value.has(key) {
        if type(value[key]) == "map" {
            let coerced: map = value[key];
            result = coerced;
        }
    }
    result
}

fn ctx_array(value: map, key: string) -> array {
    let mut result: array = [];
    if value.has(key) {
        if type(value[key]) == "array" {
            let coerced: array = value[key];
            result = coerced;
        }
    }
    result
}

fn array_map(items: array, index: int) -> map {
    let mut result: map = {};
    if items.has(index) {
        if type(items[index].copy()) == "map" {
            let coerced: map = items[index].copy();
            result = coerced;
        }
    }
    result
}

// ---------------------------------------------------------------------------
// Decision builders
// ---------------------------------------------------------------------------

fn run_completed_decision(answer: string, turn: int, retry_count: int, tool_calls_used: int, events: array) -> map {
    {
        kind: "run.completed",
        answer: answer,
        turn: turn,
        retry_count: retry_count,
        tool_calls_used: tool_calls_used,
        events: events
    }
}

fn run_failed_decision(code: string, message: string, turn: int, retry_count: int, tool_calls_used: int, error: map) -> map {
    let mut payload: map = error;
    if !payload.has("code") {
        payload = {
            status: ctx_int(error, "status", 0),
            type: ctx_string(error, "type", "api_error"),
            code: code,
            message: message,
            param: ctx_string(error, "param", ""),
            request_id: ctx_string(error, "request_id", "")
        };
    }
    {
        kind: "run.failed",
        error: payload,
        reason: code,
        turn: turn,
        retry_count: retry_count,
        tool_calls_used: tool_calls_used,
        events: []
    }
}

fn failed_code(code: string, message: string, turn: int, retry_count: int, tool_calls_used: int) -> map {
    run_failed_decision(
        code,
        message,
        turn,
        retry_count,
        tool_calls_used,
        {
            status: 0,
            type: "invalid_request_error",
            code: code,
            message: message,
            param: "",
            request_id: ""
        }
    )
}

fn control_failed(control: map, turn: int, retry_count: int, tool_calls_used: int) -> map {
    let error: map = ctx_map(control, "error");
    let code: string = ctx_string(error, "code", "cancelled");
    run_failed_decision(code, ctx_string(error, "message", code), turn, retry_count, tool_calls_used, error)
}

// ---------------------------------------------------------------------------
// Retry classification and backoff
// ---------------------------------------------------------------------------

fn error_is_retryable(error: map) -> bool {
    let code: string = ctx_string(error, "code", "");
    let mut retryable = false;
    let mut decided = false;
    if code == "setup" {
        decided = true;
    }
    if code == "config" {
        decided = true;
    }
    if code == "adapter_unavailable" {
        decided = true;
    }
    if code == "malformed_payload" {
        decided = true;
    }
    if code == "provider_step_persist_failed" {
        decided = true;
    }
    if code == "interrupted_provider" {
        decided = true;
    }
    if code == "scripted_exhausted" {
        decided = true;
    }
    if code == "cancelled" {
        decided = true;
    }
    if code == "deadline_elapsed" {
        decided = true;
    }
    if decided == false {
        if error.has("retryable") {
            if type(error["retryable"]) == "bool" {
                let flagged: bool = error["retryable"];
                retryable = flagged;
                decided = true;
            }
        }
    }
    if decided == false {
        let status: int = ctx_int(error, "status", 0);
        let error_type: string = ctx_string(error, "type", "");
        if status == 429 {
            retryable = true;
        }
        if status == 408 {
            retryable = true;
        }
        if status >= 500 {
            if status <= 599 {
                retryable = true;
            }
        }
        if error_type == "rate_limit_error" {
            retryable = true;
        }
        if error_type == "overloaded_error" {
            retryable = true;
        }
        if error_type == "server_error" {
            retryable = true;
        }
        if error_type == "timeout_error" {
            retryable = true;
        }
    }
    retryable
}

fn backoff_delay_ms(base: int, retry_count: int, cap: int) -> int {
    let mut delay: int = base;
    let mut ceiling: int = cap;
    if delay < 0 {
        delay = 0;
    }
    if ceiling < 0 {
        ceiling = 0;
    }
    if delay > ceiling {
        delay = ceiling;
    }
    let mut step = 0;
    while step < retry_count {
        if delay >= ceiling || delay == 0 {
            break;
        }
        if delay > ceiling / 2 {
            delay = ceiling;
        } else {
            delay = delay * 2;
        }
        step += 1;
    }
    delay
}

// ---------------------------------------------------------------------------
// Canonical request / message helpers
// ---------------------------------------------------------------------------

fn text_part(text: string) -> map {
    { type: "text", text: text }
}

fn encode_arguments_json(arguments: map) -> string {
    let encoded: string = json::encode(arguments);
    encoded
}

fn tool_call_part(call: map) -> map {
    {
        type: "tool_call",
        tool_call_id: ctx_string(call, "id", ""),
        name: ctx_string(call, "name", ""),
        arguments_json: encode_arguments_json(ctx_map(call, "arguments"))
    }
}

fn user_text_message(text: string) -> map {
    let mut content: array = [];
    content[content.length] = text_part(text);
    { role: "user", content: content }
}

fn system_text_message(text: string) -> map {
    let mut content: array = [];
    content[content.length] = text_part(text);
    { role: "system", content: content }
}

fn assistant_message(text: string, calls: array) -> map {
    let mut content: array = [];
    if text != "" {
        content[content.length] = text_part(text);
    }
    let mut i = 0;
    while i < calls.length {
        content[content.length] = tool_call_part(array_map(calls, i));
        i += 1;
    }
    { role: "assistant", content: content }
}

fn tool_result_message(block: map) -> map {
    let mut content: array = [];
    content[content.length] = block;
    { role: "user", content: content }
}

fn seed_messages(context: map, messages: array) -> array {
    let mut seeded: array = [];
    let prompt: string = ctx_string(context, "coding_system_prompt", "");
    if prompt != "" {
        seeded[seeded.length] = system_text_message(prompt);
    }
    if messages.length == 0 {
        let mut text: string = "";
        if context.has("input") {
            if type(context["input"]) == "map" {
                let input: map = ctx_map(context, "input");
                text = ctx_string(input, "message", ctx_string(input, "text", ""));
            } else if type(context["input"]) == "string" {
                text = ctx_string(context, "input", "");
            }
        }
        if text != "" {
            seeded[seeded.length] = user_text_message(text);
        }
    } else {
        let mut i = 0;
        while i < messages.length {
            seeded[seeded.length] = messages[i].copy();
            i += 1;
        }
    }
    seeded
}

fn tools_from_context(context: map) -> array {
    let mut tools: array = ctx_array(context, "tools");
    if tools.length == 0 {
        tools = ctx_array(context, "tool_schemas");
    }
    tools
}

fn build_llm_request(context: map, messages: array) -> map {
    let limits: map = ctx_map(context, "limits");
    let sampling: map = ctx_map(context, "sampling");
    {
        provider: ctx_string(context, "provider", "openai"),
        model: ctx_string(context, "model", ""),
        messages: messages,
        tools: tools_from_context(context),
        tool_choice: ctx_string(context, "tool_choice", ""),
        reasoning: ctx_string(context, "reasoning", ""),
        sampling: sampling,
        max_output_tokens: ctx_int(context, "max_output_tokens", ctx_int(limits, "max_output_tokens", 0)),
        stream: false,
        provider_options: ctx_map(context, "provider_options")
    }
}

fn response_is_malformed(response: map) -> bool {
    let mut malformed = false;
    if !response.has("text") {
        if !response.has("tool_calls") {
            malformed = true;
        }
    }
    if response.has("tool_calls") {
        if type(response["tool_calls"]) != "array" {
            malformed = true;
        }
    }
    malformed
}

fn dispatch_serial(calls: array, max_tool_calls: int, tool_calls_used: int) -> map {
    let mut messages: array = [];
    let mut used: int = tool_calls_used;
    let mut i = 0;
    let mut failed: map = {};
    let mut stopped = false;
    while i < calls.length {
        if stopped == false {
            if used >= max_tool_calls {
                failed = failed_code(
                    "max_tool_calls",
                    "max_tool_calls exceeded",
                    0,
                    0,
                    used
                );
                stopped = true;
            } else {
                let control: map = agent::control_check();
                if ctx_bool(control, "ok", false) == false {
                    failed = control_failed(control, 0, 0, used);
                    stopped = true;
                } else {
                    let call: map = array_map(calls, i);
                    let dispatched: map = agent::tool_dispatch(call);
                    let after: map = agent::control_check();
                    messages[messages.length] = tool_result_message(ctx_map(dispatched, "content_block"));
                    used += 1;
                    if ctx_bool(dispatched, "ok", false) == false {
                        if ctx_bool(dispatched, "terminal", false) == true {
                            failed = run_failed_decision(
                                ctx_string(ctx_map(dispatched, "error"), "code", "tool_failed"),
                                ctx_string(ctx_map(dispatched, "error"), "message", "tool dispatch failed"),
                                0,
                                0,
                                used,
                                ctx_map(dispatched, "error")
                            );
                            stopped = true;
                        }
                    }
                    if ctx_bool(after, "ok", false) == false {
                        failed = control_failed(after, 0, 0, used);
                        stopped = true;
                    }
                }
            }
        }
        i += 1;
    }
    {
        messages: messages,
        tool_calls_used: used,
        stopped: stopped,
        failure: failed
    }
}

// ---------------------------------------------------------------------------
// Entry
// ---------------------------------------------------------------------------

pub fn run(context: map) -> map {
    let config: map = ctx_map(context, "config");
    let mut decision: map = {};
    if ctx_bool(config, "parallel", false) == true {
        decision = failed_code("unsupported_parallel", "parallel tool dispatch is not supported", 0, 0, 0);
    } else if ctx_bool(config, "task", false) == true {
        decision = failed_code("unsupported_task", "task/sub-agent dispatch is not supported", 0, 0, 0);
    } else {
        decision = run_serial_loop(context);
    }
    decision
}

fn run_serial_loop(context: map) -> map {
    let limits: map = ctx_map(context, "limits");
    let config: map = ctx_map(context, "config");
    let model: string = ctx_string(context, "model", "");
    let max_turns: int = ctx_int(limits, "max_turns", ctx_int(context, "max_turns", 8));
    let max_tool_calls: int = ctx_int(limits, "max_tool_calls", ctx_int(context, "max_tool_calls", 32));
    let max_retries: int = ctx_int(config, "max_retries", ctx_int(context, "max_retries", 2));
    let base_delay: int = ctx_int(config, "base_retry_delay_ms", 100);
    let cap_delay: int = ctx_int(config, "max_retry_delay_ms", 400);
    let mut messages: array = seed_messages(context, ctx_array(context, "messages"));
    let mut turn: int = 0;
    let mut retry_count: int = 0;
    let mut tool_calls_used: int = 0;
    let mut running = true;
    let mut decision: map = failed_code("internal", "loop did not produce a decision", 0, 0, 0);
    while running {
        let control: map = agent::control_check();
        if ctx_bool(control, "ok", false) == false {
            decision = control_failed(control, turn, retry_count, tool_calls_used);
            running = false;
        } else if turn >= max_turns {
            decision = failed_code("max_turns", "max_turns exceeded", turn, retry_count, tool_calls_used);
            running = false;
        } else {
            let mut events: array = [];
            events[events.length] = { type: "model.started", turn: turn, model: model };
            let request: map = build_llm_request(context, messages);
            let provider_result: map = agent::provider_call(request);
            let after: map = agent::control_check();
            if ctx_bool(after, "ok", false) == false {
                decision = control_failed(after, turn, retry_count, tool_calls_used);
                running = false;
            } else if ctx_bool(provider_result, "ok", false) == false {
                let error: map = ctx_map(provider_result, "error");
                if error_is_retryable(error) {
                    if retry_count >= max_retries {
                        decision = run_failed_decision(
                            "retry_exhausted",
                            "provider retry budget exhausted",
                            turn,
                            retry_count,
                            tool_calls_used,
                            {
                                status: ctx_int(error, "status", 0),
                                type: ctx_string(error, "type", "api_error"),
                                code: "retry_exhausted",
                                message: "provider retry budget exhausted",
                                param: ctx_string(error, "param", ""),
                                request_id: ctx_string(error, "request_id", "")
                            }
                        );
                        running = false;
                    } else {
                        let delay: int = backoff_delay_ms(base_delay, retry_count, cap_delay);
                        agent::sleep_ms(delay);
                        let after_sleep: map = agent::control_check();
                        if ctx_bool(after_sleep, "ok", false) == false {
                            decision = control_failed(after_sleep, turn, retry_count, tool_calls_used);
                            running = false;
                        } else {
                            retry_count += 1;
                        }
                    }
                } else {
                    let code: string = ctx_string(error, "code", "provider_error");
                    decision = run_failed_decision(code, ctx_string(error, "message", code), turn, retry_count, tool_calls_used, error);
                    running = false;
                }
            } else {
                let response: map = ctx_map(provider_result, "response");
                if response_is_malformed(response) {
                    decision = failed_code("malformed_payload", "provider response is malformed", turn, retry_count, tool_calls_used);
                    running = false;
                } else {
                    let text: string = ctx_string(response, "text", "");
                    let calls: array = ctx_array(response, "tool_calls");
                    let mut completed_events: array = [];
                    completed_events[completed_events.length] = {
                        type: "model.completed",
                        turn: turn,
                        text: text,
                        tool_calls: calls.length
                    };
                    retry_count = 0;
                    if calls.length == 0 {
                        decision = run_completed_decision(text, turn + 1, retry_count, tool_calls_used, completed_events);
                        running = false;
                    } else {
                        messages[messages.length] = assistant_message(text, calls);
                        let dispatched: map = dispatch_serial(calls, max_tool_calls, tool_calls_used);
                        let produced: array = ctx_array(dispatched, "messages");
                        let mut j = 0;
                        while j < produced.length {
                            messages[messages.length] = array_map(produced, j);
                            j += 1;
                        }
                        tool_calls_used = ctx_int(dispatched, "tool_calls_used", tool_calls_used);
                        if ctx_bool(dispatched, "stopped", false) == true {
                            decision = ctx_map(dispatched, "failure");
                            running = false;
                        } else {
                            turn += 1;
                        }
                    }
                }
            }
        }
    }
    decision
}
